apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-helloworld.fullname" . }}
  labels:
    {{- include "react-helloworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "react-helloworld.selectorLabels" . | nindent 4 }}
