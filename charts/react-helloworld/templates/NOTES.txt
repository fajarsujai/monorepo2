react-helloworld has been deployed.

Check status with:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Port-forward to test locally:
  kubectl port-forward svc/{{ include "react-helloworld.fullname" . }} 3000:{{ .Values.service.port }}
  curl http://localhost:3000/
